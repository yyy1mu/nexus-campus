-- V2__nexus_campus_init.sql
-- Nexus Campus Spring Boot initial migration.
-- Existing tables (users, api_keys, etc.) were created by Flarum and are
-- managed externally.  This file serves as the Flyway baseline anchor.
-- Future migrations (V3, V4, ...) will add new Nexus-specific tables here.

-- No schema changes in this version.
